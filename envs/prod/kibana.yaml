---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kibana
  namespace: flux-system
spec:
  interval: 60s
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./envs/prod/kibana
  prune: true
  wait: true
  dependsOn:
    - name: elasticsearch-patch
      namespace: flux-system
