---
apiVersion: v1
kind: Service
metadata:
  name: pgadmin-svc
  annotations:
    tailscale.com/expose: "true"
spec:
  type: ClusterIP
  selector:
    app: pgadmin
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 80
