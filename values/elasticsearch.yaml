replicas: 1
minimumMasterNodes: 1
secret:
  enabled: false
envFrom:
  - secretRef:
      name: elasticsearch-secret
resources:
  requests:
    cpu: "250m"
    memory: "500Mi"
  limits:
    cpu: "250m"
    memory: "500Mi"
volumeClaimTemplate:
  resources:
    requests:
      storage: 3Gi
