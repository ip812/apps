replicas: 1
minimumMasterNodes: 1
secret:
  enabled: false
envFrom:
  - secretRef:
      name: elasticsearch-secret
resources:
  requests:
    cpu: "500m"
    memory: "800Mi"
  limits:
    cpu: "500m"
    memory: "800Mi"
volumeClaimTemplate:
  resources:
    requests:
      storage: 3Gi
